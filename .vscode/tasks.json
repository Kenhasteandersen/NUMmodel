{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/Fortran"
            },
            "group": "build",
        }
        ,
        {
            "label": "make debugging",
            "type": "shell",
            "command": "make debugging",
            "options": {
                "cwd": "${workspaceFolder}/Fortran"
            },
            "group": "build",
        }
    ]
} 